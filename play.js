const net = require("net");
// const playConnect = require('./play');
const { connect } = require('./client');

// establishes a connection with the game server


console.log("Connecting ...");
connect();
